export type ActinTag =  'a' | 'button';

export enum ButtonVariant {
    primary = 'primary',
    outLine = 'outline',
    secondary = 'secondary',
    link = 'link',
}

export enum ButtonType {
    button = 'button',
    submit = 'submit'
}